edad = int(input("Ingresa tu edad: "))

# Tiene permiso de los padres
permiso = False

# Usamos AND: debe tener al menos 18 años y permiso (aunque aquí no es necesario)
if edad >= 18 and permiso:
    print("Puede entrar solo a la película para adultos.")
# Usamos OR: si es mayor de edad o tiene permiso, puede entrar
elif edad >= 18 or permiso:
    print("Puede entrar con permiso.")
# Usamos NOT: si no tiene permiso
elif not permiso:
    print("No puede entrar, necesita permiso.")
